# F0.9: Create Base Layout Components (Header, Footer)

**Sprint:** 0
**Type:** Frontend - Components
**Priority:** Medium
**Status:** Completed
**Branch:** feature/sprint0-F0.9-layout-components
**Created:** 2026-02-05
**Dependencies:** F0.2 (Tailwind), F0.3 (Shadcn/UI)

---

## Description

Create the base layout components (Header and Footer) for the MemeStore application. These components will provide consistent navigation and branding across all pages.

---

## Acceptance Criteria

- [x] Header component with logo and navigation placeholder
- [x] Footer component with copyright and links placeholder
- [x] Components integrated into root layout
- [x] Responsive design (mobile-friendly)
- [x] Unit tests for components
- [x] E2E test updated to verify layout

---

## Files to Create/Modify

| File | Purpose |
|------|---------|
| `frontend/components/layout/Header.tsx` | Header component |
| `frontend/components/layout/Footer.tsx` | Footer component |
| `frontend/components/layout/index.ts` | Public exports |
| `frontend/components/layout/Header.test.tsx` | Header tests |
| `frontend/components/layout/Footer.test.tsx` | Footer tests |
| `frontend/app/layout.tsx` | Integrate Header/Footer |
| `frontend/e2e/home.spec.ts` | Update E2E tests |

---

## Implementation Steps

### Step 1: Create Header Component
1. Create layout directory
2. Create Header with:
   - Logo/brand name (MemeStore)
   - Navigation placeholder (Home, Products, Cart)
   - Responsive mobile menu (hamburger icon for mobile)

### Step 2: Create Footer Component
1. Create Footer with:
   - Copyright notice
   - Links placeholder (About, Contact, Terms)
   - Simple responsive layout

### Step 3: Create Exports
1. Create index.ts with public exports

### Step 4: Integrate into Layout
1. Update app/layout.tsx to include Header and Footer
2. Ensure proper flex layout (header top, content grows, footer bottom)

### Step 5: Write Tests
1. Unit tests for Header (renders, has navigation)
2. Unit tests for Footer (renders, has copyright)
3. Update E2E test to verify header/footer presence

---

## Component Patterns

```typescript
// Header.tsx
export function Header() {
  return (
    <header className="border-b">
      <div className="container mx-auto px-4 h-16 flex items-center justify-between">
        <span className="font-bold text-xl">MemeStore</span>
        <nav className="hidden md:flex gap-6">
          {/* Nav links */}
        </nav>
      </div>
    </header>
  );
}
```

---

## Definition of Done

- [x] Header and Footer components created
- [x] Components integrated into layout
- [x] Responsive on mobile/desktop
- [x] Unit tests pass (8 new tests)
- [x] E2E tests pass (2 new tests)
- [x] Build succeeds

---

*Ticket created: 2026-02-05*
